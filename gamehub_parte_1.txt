<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameHub — Jogos</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&family=Rajdhani:wght@300;400;500;600;700&display=swap');
        :root {
            --neon-yellow: #f7e600;
            --neon-pink: #ff2a6d;
            --neon-cyan: #05d9e8;
            --neon-green: #39ff14;
            --dark-bg: #0a0a0f;
            --darker-bg: #050508;
            --card-bg: #111118;
            --card-hover: #1a1a24;
            --text-primary: #ffffff;
            --text-secondary: #8a8a9a;
            --text-muted: #4a4a5a;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Rajdhani', sans-serif;
            background: var(--dark-bg);
            color: var(--text-primary);
            overflow-x: hidden;
            position: relative;
        }
        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0,0,0,0.15) 2px, rgba(0,0,0,0.15) 4px);
            pointer-events: none;
            z-index: 9999;
            opacity: 0.3;
        }
        @keyframes glitch {
            0% { clip-path: inset(40% 0 61% 0); transform: translate(-2px, 2px); }
            20% { clip-path: inset(92% 0 1% 0); transform: translate(2px, -2px); }
            40% { clip-path: inset(43% 0 1% 0); transform: translate(-2px, 2px); }
            60% { clip-path: inset(25% 0 58% 0); transform: translate(2px, -2px); }
            80% { clip-path: inset(54% 0 7% 0); transform: translate(-2px, 2px); }
            100% { clip-path: inset(58% 0 43% 0); transform: translate(2px, -2px); }
        }
        @keyframes glitch-2 {
            0% { clip-path: inset(65% 0 31% 0); transform: translate(2px, -2px); }
            20% { clip-path: inset(12% 0 83% 0); transform: translate(-2px, 2px); }
            40% { clip-path: inset(89% 0 3% 0); transform: translate(2px, -2px); }
            60% { clip-path: inset(3% 0 94% 0); transform: translate(-2px, 2px); }
            80% { clip-path: inset(67% 0 29% 0); transform: translate(2px, -2px); }
            100% { clip-path: inset(23% 0 73% 0); transform: translate(-2px, 2px); }
        }
        @keyframes flicker {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
            52% { opacity: 0.3; }
            54% { opacity: 1; }
        }
        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 5px var(--neon-yellow), 0 0 10px var(--neon-yellow); }
            50% { box-shadow: 0 0 20px var(--neon-yellow), 0 0 40px var(--neon-yellow); }
        }
        @keyframes slide-up {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }
        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .navbar {
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            background: rgba(10,10,15,0.9);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(247,230,0,0.15);
            padding: 0.8rem 3rem;
        }
        .nav-container {
            max-width: 1600px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.6rem;
            font-weight: 900;
            color: var(--neon-yellow);
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            text-shadow: 0 0 10px rgba(247,230,0,0.5);
            position: relative;
        }
        .logo::after {
            content: 'GAMEHUB';
            position: absolute;
            left: 2px; top: 2px;
            color: var(--neon-pink);
            opacity: 0.6;
            z-index: -1;
            animation: glitch 3s infinite linear alternate-reverse;
        }
        .nav-links {
            display: flex;
            gap: 2.5rem;
            list-style: none;
        }
        .nav-links a {
            font-family: 'Orbitron', sans-serif;
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--text-secondary);
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: all 0.3s;
            position: relative;
            padding: 0.5rem 0;
        }
        .nav-links a:hover {
            color: var(--neon-yellow);
            text-shadow: 0 0 8px rgba(247,230,0,0.6);
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0; left: 0;
            width: 0; height: 2px;
            background: var(--neon-yellow);
            transition: width 0.3s;
            box-shadow: 0 0 8px var(--neon-yellow);
        }
        .nav-links a:hover::after { width: 100%; }
        .nav-actions {
            display: flex;
            gap: 1rem;
            align-items: center;
        }
        .btn-cyber {
            font-family: 'Orbitron', sans-serif;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            padding: 0.7rem 1.8rem;
            border: none;
            cursor: pointer;
            position: relative;
            clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
            transition: all 0.3s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .btn-cyber-primary {
            background: var(--neon-yellow);
            color: var(--dark-bg);
        }
        .btn-cyber-primary:hover {
            background: #fff;
            box-shadow: 0 0 20px rgba(247,230,0,0.6), 0 0 40px rgba(247,230,0,0.3);
            transform: translateY(-2px);
        }
        .btn-cyber-secondary {
            background: transparent;
            color: var(--neon-yellow);
            border: 2px solid var(--neon-yellow);
        }
        .btn-cyber-secondary:hover {
            background: rgba(247,230,0,0.1);
            box-shadow: 0 0 15px rgba(247,230,0,0.4);
        }
        .btn-cyber-outline {
            background: transparent;
            color: var(--text-secondary);
            border: 1px solid rgba(255,255,255,0.2);
            clip-path: polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px);
        }
        .btn-cyber-outline:hover {
            border-color: var(--neon-cyan);
            color: var(--neon-cyan);
            box-shadow: 0 0 10px rgba(5,217,232,0.3);
        }
        .hero {
            min-height: 85vh;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: flex-end;
            padding: 0 3rem 4rem;
            margin-top: 60px;
        }
        .hero-bg {
            position: absolute;
            inset: 0;
            background: 
                radial-gradient(ellipse at 20% 80%, rgba(247,230,0,0.08) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 20%, rgba(255,42,109,0.06) 0%, transparent 50%),
                radial-gradient(ellipse at 50% 50%, rgba(5,217,232,0.04) 0%, transparent 60%);
        }
        .hero-carousel {
            position: relative;
            width: 100%;
            max-width: 1600px;
            margin: 0 auto;
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }
        .carousel-main {
            flex: 1;
            position: relative;
            border: 1px solid rgba(247,230,0,0.2);
            clip-path: polygon(20px 0, 100% 0, 100% calc(100% - 20px), calc(100% - 20px) 100%, 0 100%, 0 20px);
            overflow: hidden;
            background: var(--card-bg);
            height: 500px;
        }
        .carousel-main::before {
            content: '';
            position: absolute;
            inset: 0;
            border: 1px solid var(--neon-yellow);
            opacity: 0.3;
            pointer-events: none;
            z-index: 10;
        }
        .carousel-content {
            position: absolute;
            bottom: 0;
            left: 0; right: 0;
            padding: 3rem;
            background: linear-gradient(to top, rgba(10,10,15,0.95) 0%, transparent 100%);
            z-index: 5;
        }
        .carousel-badge {
            display: inline-block;
            font-family: 'Orbitron', sans-serif;
            font-size: 0.65rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            padding: 0.4rem 1rem;
            background: var(--neon-yellow);
            color: var(--dark-bg);
            clip-path: polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px);
            margin-bottom: 1rem;
        }
        .carousel-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 2.5rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 0.5rem;
            text-shadow: 0 0 20px rgba(0,0,0,0.8);
            position: relative;
        }
        .carousel-title .glitch-layer {
            position: absolute;
            top: 0; left: 0;
            color: var(--neon-pink);
            opacity: 0.7;
            animation: glitch-2 4s infinite linear alternate-reverse;
            clip-path: inset(0 0 0 0);
        }
        .carousel-desc {
            font-size: 1.1rem;
            color: var(--text-secondary);
            max-width: 500px;
            line-height: 1.6;
        }
        .carousel-side {
            width: 280px;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        .side-card {
            position: relative;
            height: 160px;
            border: 1px solid rgba(255,255,255,0.1);
            clip-path: polygon(12px 0, 100% 0, 100% calc(100% - 12px), calc(100% - 12px) 100%, 0 100%, 0 12px);
            overflow: hidden;
            cursor: pointer;
            transition: all 0.3s;
            background: var(--card-bg);
        }
        .side-card:hover {
            border-color: var(--neon-cyan);
            transform: translateX(-5px);
            box-shadow: -5px 0 20px rgba(5,217,232,0.2);
        }
        .side-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.7;
            transition: opacity 0.3s;
        }
        .side-card:hover img { opacity: 1; }
        .side-card-title {
            position: absolute;
            bottom: 0;
            left: 0; right: 0;
            padding: 1rem;
            background: linear-gradient(to top, rgba(10,10,15,0.9), transparent);
            font-family: 'Orbitron', sans-serif;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .carousel-nav {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            width: 50px;
            height: 50px;
            background: rgba(10,10,15,0.8);
            border: 1px solid var(--neon-yellow);
            color: var(--neon-yellow);
            font-size: 1.5rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
            transition: all 0.3s;
            z-index: 20;
        }
        .carousel-nav:hover {
            background: var(--neon-yellow);
            color: var(--dark-bg);
            box-shadow: 0 0 20px rgba(247,230,0,0.5);
        }
        .carousel-prev { left: -25px; }
        .carousel-next { right: -25px; }
        .carousel-indicators {
            display: flex;
            gap: 0.5rem;
            position: absolute;
            bottom: -2rem;
            left: 50%;
            transform: translateX(-50%);
        }
        .carousel-dot {
            width: 40px;
            height: 4px;
            background: rgba(255,255,255,0.2);
            cursor: pointer;
            transition: all 0.3s;
            clip-path: polygon(2px 0, 100% 0, 100% calc(100% - 2px), calc(100% - 2px) 100%, 0 100%, 0 2px);
        }
        .carousel-dot.active {
            background: var(--neon-yellow);
            box-shadow: 0 0 10px var(--neon-yellow);
        }
        .carousel-dot:hover {
            background: rgba(247,230,0,0.5);
        }
        .marquee-bar {
            background: var(--neon-yellow);
            color: var(--dark-bg);
            padding: 0.6rem 0;
            overflow: hidden;
            position: relative;
        }
        .marquee-content {
            display: flex;
            animation: marquee 20s linear infinite;
            white-space: nowrap;
            font-family: 'Orbitron', sans-serif;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 3px;
        }
        .marquee-content span { padding: 0 3rem; }

        .section {
            padding: 4rem 3rem;
            max-width: 1600px;
            margin: 0 auto;
        }
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 2.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid rgba(247,230,0,0.2);
        }
        .section-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.8rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 3px;
            position: relative;
        }
        .section-title::after {
            content: '';
            position: absolute;
            bottom: -1rem;
            left: 0;
            width: 60px;
            height: 3px;
            background: var(--neon-yellow);
            box-shadow: 0 0 10px var(--neon-yellow);
        }
        .section-link {
            font-family: 'Orbitron', sans-serif;
            font-size: 0.7rem;
            font-weight: 600;
            color: var(--neon-green);
            text-transform: uppercase;
            letter-spacing: 2px;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s;
        }
        .section-link:hover {
            color: var(--neon-yellow);
            text-shadow: 0 0 10px rgba(247,230,0,0.5);
        }
        .games-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        .game-card {
            background: var(--card-bg);
            border: 1px solid rgba(255,255,255,0.08);
            clip-path: polygon(15px 0, 100% 0, 100% calc(100% - 15px), calc(100% - 15px) 100%, 0 100%, 0 15px);
            overflow: hidden;
            transition: all 0.4s;
            cursor: pointer;
            position: relative;
        }
        .game-card::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent 40%, rgba(247,230,0,0.03) 100%);
            opacity: 0;
            transition: opacity 0.4s;
            pointer-events: none;
            z-index: 1;
        }
        .game-card:hover::before { opacity: 1; }
        .game-card:hover {
            border-color: var(--neon-yellow);
            transform: translateY(-8px);
            box-shadow: 0 10px 40px rgba(247,230,0,0.1), 0 0 60px rgba(247,230,0,0.05);
        }
        .game-card-image {
            width: 100%;
            height: 340px;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .game-card-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.5s;
            filter: contrast(1.1);
        }
        .game-card:hover .game-card-image img {
            transform: scale(1.08);
            filter: contrast(1.2) saturate(1.3);
        }
        .game-card-badge {
            position: absolute;
            top: 1rem;
            left: 1rem;
            font-family: 'Orbitron', sans-serif;
            font-size: 0.6rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            padding: 0.3rem 0.8rem;
            clip-path: polygon(6px 0, 100% 0, 100% calc(100% - 6px), calc(100% - 6px) 100%, 0 100%, 0 6px);
            z-index: 5;
        }